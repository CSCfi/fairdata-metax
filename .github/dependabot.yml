version: 2
updates:
  - package-ecosystem: "pip"
    directory: "/"
    schedule:
      interval: "monthly"
    allow:
      # Allow both direct and indirect updates for all packages
      - dependency-type: "all"
